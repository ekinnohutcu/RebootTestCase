using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlugManager : MonoBehaviour
{
    // Start is called before the first frame update
    private int _plugCount = 1;
    
    
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
